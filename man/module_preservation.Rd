% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_preservation.R
\name{module_preservation}
\alias{module_preservation}
\title{Test whether co-expression modules are preserved across species}
\usage{
module_preservation(
  modules_ref,
  net_ref,
  net_test,
  orthologs = NULL,
  map = NULL,
  edges = NULL,
  cliques = NULL,
  sp_ref = NULL,
  sp_test = NULL,
  n_perm = 10000L,
  min_module_size = 10L,
  binary = FALSE,
  alpha = 0.05,
  qvalue_method = c("liang", "bh"),
  sensitivity = FALSE,
  n_cores = 1L,
  seed = NULL
)
}
\arguments{
\item{modules_ref}{Module detection result for the reference species
(output of \code{\link[=detect_modules]{detect_modules()}}).}

\item{net_ref, net_test}{Network objects from \code{\link[=compute_network]{compute_network()}} for the
reference and test species.}

\item{orthologs}{Data frame with columns \code{Species1}, \code{Species2}, \code{hog}.
Ignored when \code{map} is supplied.}

\item{map}{Optional pre-built ortholog map from \code{\link[=resolve_ortholog_map]{resolve_ortholog_map()}}.}

\item{edges, cliques}{Optional \code{\link[=find_coexpressologs]{find_coexpressologs()}} and \code{\link[=find_cliques]{find_cliques()}}
results, passed to \code{\link[=resolve_ortholog_map]{resolve_ortholog_map()}} for paralog resolution.}

\item{sp_ref, sp_test}{Species labels, required only when \code{edges} or
\code{cliques} is supplied.}

\item{n_perm}{Number of permutations (default 10000). The smallest
attainable p-value is \code{1 / (n_perm + 1)}, so this sets the floor on how
significant any module can be: at 1000 permutations every strongly
preserved module ties at \code{p = 0.000999} and cannot be ranked by q-value.
The permutation loop costs O(edges) per iteration -- roughly 0.7 s per
1000 permutations on a 6000-gene network at density 0.03 -- so the
default buys a floor of 1e-4 cheaply. Raise it further when many modules
sit at the floor.}

\item{min_module_size}{Minimum number of mapped test-species genes for a
module to be tested (default 10).}

\item{binary}{Treat every surviving edge as weight 1 (default \code{FALSE}).
On a hard-thresholded network this changes little and makes \code{avg.weight}
exactly the module edge density.}

\item{alpha}{Significance threshold (default 0.05).}

\item{qvalue_method}{Multiple-testing correction. \code{"liang"} (default) uses
\code{\link[DiscreteQvalue:DQ]{DiscreteQvalue::DQ()}} with the exact discrete support of a permutation
p-value, \verb{\{1/(n_perm+1), ..., 1\}} -- the same treatment
\code{\link[=permutation_hog_test]{permutation_hog_test()}} gives its permutation p-values. \code{"bh"} forces
Benjamini-Hochberg. Liang falls back to BH automatically when there are
too few modules to estimate pi0 (fewer than 10) or if the estimator
fails.}

\item{sensitivity}{Re-run under a naive ortholog map -- one built from
\code{orthologs} alone, with no clique or coexpressolog resolution -- and
report both results side by side (default \code{FALSE}). Doubles the runtime.
Because resolution may only choose which paralog copy carries a label,
the two runs must map the identical set of test-species genes; the
returned table records whether they did. A large \code{Zsummary} gap with
matching gene sets means the copy choice mattered; a mismatched gene set
means the resolution layer is filtering rather than choosing, which is a
bug.}

\item{n_cores}{Number of OpenMP threads (default 1).}

\item{seed}{Optional RNG seed. Results are independent of \code{n_cores}.}
}
\value{
A list with components:
\describe{
\item{preservation}{One row per tested module: \code{module}, \code{size},
\code{size_mapped}, the two headline statistics, their permutation
p-values, the combined \code{p.value} and \code{q.value}, \code{Z.avg.weight},
\code{Z.cor.degree}, \code{Zsummary} and \code{medianRank}.}
\item{observed}{All six statistics per module, with permutation means
and standard deviations.}
\item{map}{The ortholog map used.}
\item{sensitivity}{Only when \code{sensitivity = TRUE}: per-module
\code{Zsummary} and \code{q.value} under the resolved and naive maps, plus the
\code{same_gene_set} attribute recording whether both mapped the identical
test-species genes.}
\item{params}{Call parameters.}
}
}
\description{
Projects the modules of a reference species onto a test species through an
ortholog map and tests, per module, whether the module's topology survives
in the test species' network. Unlike gene-overlap tests, a module that keeps
its gene membership but loses its internal wiring is correctly reported as
diverged.
}
\section{Statistics}{

Two statistics carry the call, the pair NetRep computes when only an
adjacency matrix is available:
\describe{
\item{avg.weight}{\code{sum(kIM) / (m^2 - m)}, the module's mean within-module
adjacency ("module density"). On a binary network this is the
proportion of realised to possible within-module edges.}
\item{cor.degree}{Pearson correlation between each gene's intramodular
connectivity in the reference network and in the test network. Measures
whether hub identity is conserved.}
}
\code{meanClusterCoeff} and \code{meanMAR} are reported in \code{observed} as diagnostics
but take no part in the call; on a hard-thresholded network the surviving
edge weights are nearly constant, which leaves both statistics with almost
no dynamic range.
}

\section{Null model}{

Gene identities are shuffled while edges are held constant, and each module
is handed a contiguous block of the shuffled genes of its own size. Only
ortholog-mappable test-species genes enter the shuffle, matching NetRep's
\code{"overlap"} null model. P-values are one-sided
(\code{(exceedances + 1) / (permutations + 1)}) and combined across the two
statistics with \code{pmax}, so a module is called preserved only when both are
significant -- the same reciprocal criterion as
\code{pval_combine = "max"} elsewhere in the package.
}

\section{Paralog resolution}{

Multi-copy HOGs are reduced to one counterpart per gene by
\code{\link[=resolve_ortholog_map]{resolve_ortholog_map()}} when \code{map} is not supplied. Resolution only
chooses which paralog copy carries a module label; it never changes which
genes are mappable, so the tested gene set does not depend on the
conservation evidence used to resolve it.
}

\examples{
\dontrun{
pres <- module_preservation(mods_A, net_A, net_B, ortho,
  edges = rcx$edges, cliques = rcx$cliques,
  sp_ref = "SP_A", sp_test = "SP_B"
)
classify_preservation(pres)
}

}
\references{
Ritchie, S. C. et al. (2016). A scalable permutation approach reveals
replication and preservation patterns of network modules in large datasets.
\emph{Cell Systems}, 3(1), 71-82. \doi{10.1016/j.cels.2016.06.012}

Langfelder, P., Luo, R., Oldham, M. C. & Horvath, S. (2011). Is my network
module preserved and reproducible? \emph{PLoS Computational Biology}, 7(1),
e1001057. \doi{10.1371/journal.pcbi.1001057}
}
