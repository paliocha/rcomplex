% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preservation_matrix.R
\name{preservation_matrix_test}
\alias{preservation_matrix_test}
\title{Trait relabelling test on the all-pairs preservation matrix}
\usage{
preservation_matrix_test(
  classification,
  group,
  block = NULL,
  statistic = c("zsummary", "zsummary_raw"),
  exclude_within_block = TRUE,
  n_perm = NULL,
  enum_max = 50000L,
  n_perm_pres = NULL
)
}
\arguments{
\item{classification}{The \code{classification} data frame from
\code{\link[=preservation_paired]{preservation_paired()}}, ideally over an all-pairs \code{pairs} table (see
\code{\link[=all_species_pairs]{all_species_pairs()}}). Must carry \code{reference}, \code{test} and the effect
column named by \code{statistic}; \code{q.value} is used only for the
\verb{$saturation} diagnostic. Rows whose effect is \code{NA} --- \code{"untested"}
modules, and any module whose null correlation was unavailable --- are
dropped, since nothing was measured for them.}

\item{group}{Named vector mapping species to trait values. Any number of
levels is allowed. Species not appearing in \code{classification} are ignored,
so the permuted label multiset is the one the matrix actually holds.}

\item{block}{Optional named vector mapping species to a phylogenetic group
(a genus, say). Supplying it enables the restricted within-block null and
the within-block exclusion. With \code{block = NULL} only the free null is
run and \code{exclude_within_block} has nothing to act on.}

\item{statistic}{Which effect column to average. \code{"zsummary"} (default)
uses \code{Zsummary_std}, which has unit variance under the permutation null
and is therefore comparable across modules and contrasts; \code{"zsummary_raw"}
uses \code{Zsummary}, whose null spread depends on the correlation between the
two preservation statistics and so differs from module to module. There
is deliberately no q-value option.}

\item{exclude_within_block}{Drop rows whose two species share a block
(default \code{TRUE}). Ignored when \code{block} is \code{NULL}.}

\item{n_perm}{Number of labellings to draw when a space is too large to
enumerate. \code{NULL} (default) means 10000 if it comes to that. Ignored, with
a message when it was supplied explicitly, for a space that is enumerated.
The sampled branch draws from the global RNG, so call
\code{set.seed()} beforehand for a reproducible p-value. Which
branch runs is decided by \code{enum_max} rather than by the
caller, so a design can cross into the sampled regime
without the call site changing.}

\item{enum_max}{Enumerate a null while its space holds at most this many
labellings (default 50000). Lower it to cap runtime on wide designs; the
free space grows as the multinomial coefficient, so 20 species split
10/10 is already 184756. A sampled null reports its floor over draws,
not over the labelling space --- see the last paragraph of \emph{The two
nulls}.}

\item{n_perm_pres}{The \code{n_perm} used in the \code{\link[=module_preservation]{module_preservation()}} runs
behind \code{classification}, if known. Nothing in the test reads it; it lets
\verb{$saturation} report \code{q_floor_global}, the smallest q-value a \emph{global}
Benjamini-Hochberg correction over the whole matrix could reach,
\code{n_tests / (n_perm_pres + 1)}. \code{NULL} (default) leaves that field \code{NA}.}
}
\value{
A list with components:
\describe{
\item{observed}{The statistic under the true labelling.}
\item{statistic}{Echo of the input.}
\item{form}{\code{"difference"} for two trait levels, \code{"dispersion"}
otherwise.}
\item{class_means}{Data frame with \code{class}, \code{n} and \code{mean_z}: the
observed class means the statistic is built from.}
\item{rows_per_pair}{Data frame with \code{sp1}, \code{sp2}, \code{concordant},
\code{same_block} and \code{n}, one row per species pair contributing.}
\item{free}{List for the free null: \code{null_distribution}, \code{p_value},
\code{p_min}, \code{p_attainable}, \code{n_tied_max}, \code{n_distinct}, \code{rank},
\code{exact}, \code{n_labellings}, \code{n_scored}. \code{p_attainable} is the tie-aware
floor --- labellings sharing a statistic cannot be separated, so
nothing scores below the tail of the maximum --- and \code{n_tied_max}
says how many labellings that tail holds. When \code{exact} is \code{FALSE}
both are counted over the \code{n_scored} draws rather than over the
\code{n_labellings} of the space.}
\item{blocked}{The same list for the within-block null, or \code{NULL} when
\code{block} was not supplied.}
\item{p_free,p_blocked}{The two p-values, lifted out for convenience.
\code{p_blocked} is \code{NA} without a \code{block}.}
\item{saturation}{List describing the resolution of the supplied
q-values: \code{n_tests}, \code{n_distinct}, \code{q_floor}, \code{n_at_floor},
\code{n_at_one}, plus \code{p_min_pres} and \code{q_floor_global} (both \code{NA}
without \code{n_perm_pres}). \code{n_distinct}, \code{n_at_floor} and \code{n_at_one}
are counted with the tolerance \code{\link[=pvalue_resolution]{pvalue_resolution()}} uses, so
the two agree on one q-vector --- relative for \code{n_distinct} and
\code{n_at_floor}, and a one-sided window below 1 for \code{n_at_one},
which counts any q-value at or above 1 so that invalid input is
reported rather than dropped:
values within a few last bits of each other are one value
wobbled by arithmetic, not two resolvable ones. \code{n_tests}
counts every row of the supplied
\code{classification} carrying a finite q-value --- the multiplicity
population is the whole matrix, so this is deliberately \emph{not}
\code{n_rows} below, which is the subset this test averages over after
dropping unmeasured rows and within-block pairs. Reported next to
the p-values so the reader can see how much of the input's
significance scale is collapsed.}
\item{n_rows,n_pairs,n_excluded}{Rows used, distinct species pairs
behind them, and rows dropped for sharing a block.}
\item{species,group,block}{The design actually tested.}
}
}
\description{
Asks whether modules are less well preserved between species that differ
in a trait than between species that share it, using every contrast in an
all-pairs \code{\link[=preservation_paired]{preservation_paired()}} run. The null relabels species, holding
the label counts and the whole preservation matrix fixed.
}
\section{Why the effect size and not the q-value}{

The statistic ranks contrasts by \code{Zsummary_std}, never by \code{p.value} or
\code{q.value}. A permutation p-value saturates: on the eight-species Pooideae
set with \code{n_perm = 2000} and 511 module-directions, \code{q.value} takes 172
distinct values, 35 modules sit tied at the floor of 0.00071 and 20 at
exactly 1. Among those 35 floored modules \code{Zsummary_std} spans 6.4 to
66.7 --- a tenfold range of effect that the p-value cannot see at all.
Anything that ranks, weights or orders modules must therefore read the
standardised effect, which is continuous; p and q are for the significance
\emph{call} only. \verb{$saturation} reports how far the supplied q-values are
collapsed, so a caller who reaches for them anyway can see what resolution
is left.
}

\section{The statistic}{

Each row of \code{classification} is one module in one direction of one
contrast, and its two species are trait-\emph{concordant} when they share a
trait value and \emph{discordant} otherwise. With two trait levels the
statistic is
\code{mean(Zsummary_std | concordant) - mean(Zsummary_std | discordant)},
so a \strong{positive} value means discordant pairs are the less preserved
ones, which is the biological hypothesis, and the test is one-sided upward.

With more than two levels a single difference no longer describes the
design, because discordance is no longer one thing. The classes are then
the concordant rows (pooled across levels) plus one class per unordered
pair of distinct levels, and the statistic is the row-weighted standard
deviation of the class means --- their between-class dispersion, on the
\code{Zsummary_std} scale. Larger is still more structure, so the test stays
one-sided upward, but it is no longer signed and does not say which
direction the difference runs; read \verb{$class_means} for that. Because the
concordant classes are pooled, a two-level trait has exactly two classes
and the dispersion form is never used for it: the binary case is the
difference above, exactly.

Every module-direction counts once, so a contrast with many modules
contributes more than one with few. That is deliberate --- the unit is the
module, and a species pair with more modules carries more evidence --- but
it means a single hugely partitioned species can dominate; check
\verb{$rows_per_pair}.
}

\section{The two nulls}{

\code{p_free} permutes trait labels over all species, preserving the label
counts (\code{choose(8, 4) = 70} labellings for a balanced binary trait on
eight species). \code{p_blocked} permutes only \emph{within} each \code{block}, which
holds the phylogeny fixed and is the conservative, phylogenetically
controlled null (16 labellings for four genera of two). Both are reported
because their agreement is the diagnostic: a free p-value far smaller than
the blocked one means the larger label space bought its resolution by
breaking phylogenetic control rather than by measuring the trait.

Neither floor is \code{1 / n_labellings}, because relabellings that leave every
pair's concordance alone reproduce the statistic exactly. Swapping the two
trait values everywhere is one such, so a binary design always carries at
least a twofold tie at the maximum: 2/70 = 0.029 free on eight species
split 4/4, and 2/16 = 0.125 within four blocks of two --- which is why the
restricted null of a four-genus paired design cannot reach alpha on its
own. With more levels the dispersion cannot tell the level \emph{names} apart
either, so every renaming of the levels that preserves the label counts
ties as well. That is \verb{g!} renamings only when the design is balanced;
in general it is \code{prod(factorial(table(table(labels))))}, since a renaming
has to map each level onto one of the same size. Balanced three levels on
six species tie 6 of 90 labellings, a floor of 0.067; an unbalanced 2/2/1
split on five species ties only 2 of 30 --- the same 0.067, but from two
ties rather than six. \code{p_attainable} reports whichever floor actually
binds and \code{n_tied_max} how many labellings share it; the function warns
when that floor exceeds 0.05, since no signal can then reach significance.

Both floors describe an \emph{enumerated} null. When a space is too large to
enumerate, \code{p_min}, \code{p_attainable} and \code{n_tied_max} are counted over the
\code{n_perm} draws instead: \code{p_min} is then the sampling floor
\code{1 / (n_perm + 1)}, which can sit far below the labelling-space floor the
design really imposes, and \code{n_tied_max} counts drawn labellings with
repeats rather than distinct ones. \code{exact} says which regime a null is in,
and only an exact one bounds the design.
}

\section{Multiplicity scope of the q-values}{

\code{\link[=preservation_paired]{preservation_paired()}} corrects within each contrast: \code{q.value} is
Benjamini-Hochberg over that contrast's own modules, a dozen or two tests.
An all-pairs matrix reframes those contrasts as one analysis --- every
module-direction tests the same hypothesis --- so the per-contrast
correction under-corrects, by roughly the number of contrasts. Whether a
global correction is available instead is a question about the
permutation floor, not about the data: with \code{n_tests} module-directions
and a smallest attainable p-value of \code{1 / (n_perm + 1)}, BH cannot put any
module below \code{n_tests / (n_perm + 1)}. For 511 tests that is 0.26 at
\code{n_perm = 2000}, which nothing can pass, and 0.026 at \code{n_perm = 20000},
which is usable. Pass \code{n_perm_pres} and \verb{$saturation$q_floor_global}
reports that number, with a warning when it exceeds 0.05. The relabelling
test itself is untouched --- it never reads a q-value --- but any
per-module significance call taken off the same matrix is.
}

\section{Why within-block pairs are excluded}{

In a paired design such as the Pooideae one --- each genus contributing an
annual and a perennial --- every within-genus pair is trait-discordant
while every trait-concordant pair is between-genus. Trait status and
phylogenetic distance are then perfectly confounded within that subset,
and the confound runs \emph{against} the hypothesis: close relatives are better
preserved, so the within-genus discordant pairs are pulled upward and mask
a real effect. \code{exclude_within_block = TRUE} (the default) drops those
rows. The exclusion is by block membership, which no relabelling changes,
so the same rows are excluded under every labelling and the null stays
valid.
}

\examples{
\dontrun{
pairs <- all_species_pairs(names(group))
res <- preservation_paired(mods, nets, ortho, pairs, group = group)
pmt <- preservation_matrix_test(res$classification, group,
  block = genus
)
c(pmt$observed, pmt$p_free, pmt$p_blocked)
}

}
\seealso{
\code{\link[=preservation_paired]{preservation_paired()}}, \code{\link[=all_species_pairs]{all_species_pairs()}},
\code{\link[=tag_permutation]{tag_permutation()}}
}
