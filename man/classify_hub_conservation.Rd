% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules.R
\name{classify_hub_conservation}
\alias{classify_hub_conservation}
\title{Classify hub gene conservation across species and traits}
\usage{
classify_hub_conservation(
  hub_results,
  species_trait,
  module_comparisons = NULL,
  alpha = 0.05,
  jaccard_threshold = 0.1,
  min_trait_fraction = 0.5,
  correspondence_threshold = 0.5
)
}
\arguments{
\item{hub_results}{Named list keyed by species name.  Each element is the
data frame output of \code{\link[=identify_module_hubs]{identify_module_hubs()}} (with \code{orthologs}
provided so the \code{hog} column is populated).}

\item{species_trait}{Named character or factor vector mapping species to
trait groups, e.g. \code{c(SP_A = "annual", SP_B = "annual", SP_C = "perennial", SP_D = "perennial")}.}

\item{module_comparisons}{Optional named list of \code{\link[=compare_modules]{compare_modules()}} outputs
keyed by alphabetically sorted species pair (e.g. \code{"SP_A.SP_C"}).
Required for the conserved_hub vs rewired_hub distinction.}

\item{alpha}{Significance threshold for module correspondence (default 0.05).}

\item{jaccard_threshold}{Jaccard threshold for module correspondence
(default 0.1).}

\item{min_trait_fraction}{Minimum fraction of species (within a trait group)
where the HOG must be a hub for the group to count (default 0.5).}

\item{correspondence_threshold}{Fraction of cross-trait hub pairs that must
have corresponding modules for the HOG to be classified as
\code{conserved_hub} rather than \code{rewired_hub} (default 0.5).}
}
\value{
A data frame with one row per HOG:
\describe{
\item{hog}{HOG identifier}
\item{classification}{Conservation category (see Classification
waterfall)}
\item{n_species_hub}{Number of species where the HOG is a hub}
\item{n_species_present}{Number of species where the HOG has genes}
\item{hub_trait_groups}{Comma-separated trait groups where it qualifies
as hub (\code{NA} for non_hub)}
\item{n_corresponding}{Cross-trait hub pairs with corresponding modules
(\code{NA} without \code{module_comparisons})}
\item{n_cross_pairs}{Total cross-trait hub pairs checked (\code{NA} without
\code{module_comparisons})}
\item{max_centrality}{Highest centrality score across species}
\item{best_hub_species}{Species with highest centrality}
}
}
\description{
Given per-species hub identification results (from
\code{\link[=identify_module_hubs]{identify_module_hubs()}}), maps hub genes to HOGs and classifies each HOG
by its hub conservation pattern relative to a discrete trait (e.g.
annual / perennial).
}
\section{Classification waterfall}{

For each HOG that appears in at least one species:
\describe{
\item{conserved_hub}{Hub in multiple trait groups \strong{and} the hub modules
correspond across traits (checked via \code{module_comparisons}).}
\item{rewired_hub}{Hub in multiple trait groups but in
\strong{non-corresponding} modules -- the gene kept its centrality but
changed regulatory context.}
\item{multi_trait_hub}{Hub in multiple trait groups; module correspondence
unknown (\code{module_comparisons} not provided).}
\item{\link{trait}_specific_hub}{Hub in exactly one trait group (e.g.
\code{"annual_specific_hub"}).}
\item{sporadic_hub}{Hub in some species but does not reach
\code{min_trait_fraction} in any trait group.}
\item{non_hub}{Present in modules but not a hub in any species.}
}
}

\examples{
\dontrun{
hub_list <- list(
  SP_A = identify_module_hubs(mods_A, net_A, ortho_A),
  SP_B = identify_module_hubs(mods_B, net_B, ortho_B)
)
trait <- c(SP_A = "annual", SP_B = "perennial")
classify_hub_conservation(hub_list, trait)
}

}
