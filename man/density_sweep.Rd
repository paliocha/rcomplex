% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison.R
\name{density_sweep}
\alias{density_sweep}
\alias{density_sweep.default}
\title{Sweep density thresholds for robustness assessment}
\usage{
density_sweep(networks, ...)

\method{density_sweep}{default}(
  networks,
  orthologs,
  multipliers = seq(0.95, 1.05, by = 0.01),
  method = c("permutation", "analytical"),
  alternative = c("greater", "less"),
  alpha = 0.05,
  n_cores = 1L,
  use_torch = FALSE,
  min_exceedances = 50L,
  max_permutations = 10000L,
  species_pairs = NULL,
  ...
)
}
\arguments{
\item{networks}{Named list of \code{\link{compute_network}} outputs.}

\item{...}{Additional arguments passed to the default method.}

\item{orthologs}{Data frame with columns \code{Species1},
\code{Species2}, \code{hog}.}

\item{multipliers}{Numeric vector of threshold multipliers
(default \code{seq(0.95, 1.05, by = 0.01)}).}

\item{method}{Comparison method passed to
\code{\link{find_coexpressologs}}: \code{"permutation"} (default)
or \code{"analytical"}.}

\item{alternative}{\code{"greater"} (conservation, default) or
\code{"less"} (divergence).}

\item{alpha}{Significance threshold (default 0.05).}

\item{n_cores}{Number of threads (default 1).}

\item{use_torch}{Logical; GPU acceleration for permutation method
(default \code{FALSE}).}

\item{min_exceedances}{Besag-Clifford parameter for permutation
method (default 50).}

\item{max_permutations}{Maximum permutations (default 10000).}

\item{species_pairs}{Optional list of length-2 character vectors
passed to \code{\link{find_coexpressologs}} at each threshold
level. Defaults to all pairwise combinations.}
}
\value{
A data frame with columns \code{multiplier},
\code{eff_density}, \code{n_significant}, and \code{edges}
(list-column of edge data frames).
}
\description{
Reruns the full pairwise comparison pipeline at multiple threshold
multipliers to assess how sensitive conservation results are to the
choice of density cutoff.
}
\details{
For each multiplier, every network's threshold is scaled by
\code{threshold * multiplier}, the effective density is recorded, and
\code{\link{find_coexpressologs}} is called on the modified networks.
}
\examples{
\dontrun{
sweep <- density_sweep(networks, orthologs,
                        method = "permutation", use_torch = TRUE)
}

}
\seealso{
\code{\link{find_coexpressologs}},
\code{\link{clique_persistence}}
}
