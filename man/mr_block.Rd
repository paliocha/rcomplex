% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_block.R
\name{mr_block}
\alias{mr_block}
\title{Reconstruct mutual-rank values for a gene subset}
\usage{
mr_block(x, genes, net)
}
\arguments{
\item{x}{Expression matrix (genes x samples) the network was built
from. Must contain every network gene as a row; extra rows (e.g.
genes removed by the \code{min_var} filter) are ignored.}

\item{genes}{Character vector of gene identifiers (no duplicates), all
present in \code{rownames(net$network)}.}

\item{net}{Network object from \code{\link[=compute_network]{compute_network()}} (sparse or dense)
with \code{norm_method = "MR"}.}
}
\value{
A k x k numeric matrix of mutual-rank values with
\code{dimnames = list(genes, genes)} and zero diagonal, on the same scale
as the network (raw MR, or log-normalized when the network was built
with \code{mr_log_transform = TRUE}).
}
\description{
Computes the exact Mutual Rank co-expression values for a subset of
genes from the expression matrix, using the network's stored parameters
(\code{cor_method}, \code{abs_cor}, \code{mr_log_transform}). A sparse network
(\code{compute_network(sparse = TRUE)}) discards values below its
\code{store_threshold}; this function reconstructs them (for heatmaps or
other visualisations of, say, a module) without rebuilding the dense
n x n matrix: \eqn{MR_{ij} = \sqrt{R_{ij} R_{ji}}} only needs the
correlations of genes i and j ranked over all n network genes, so a
k x n correlation slice suffices.
}
\details{
The gene universe is \code{rownames(net$network)}: \code{x} is subset to those
rows first (the \code{min_var} filter was already applied when the network
was built), and ranks span exactly the network genes. Pass the same
expression matrix that built the network; the reconstructed block then
matches the dense \code{compute_network(sparse = FALSE)} matrix.

Reconstruction is exact up to ties in the correlation values: exactly
tied correlations get average ranks, and a mathematical tie can be
broken differently by this function (\code{stats::cor()}) and the network's
correlation backend (\code{Rfast::cora()}), shifting a rank by 0.5. Ties are
measure-zero for Pearson on continuous data, but Spearman correlations
lie on a finite grid where occasional ties are possible.
}
\examples{
\dontrun{
net <- compute_network(x, density = 0.03)  # sparse
# exact values for one module, including sub-threshold entries:
blk <- mr_block(x, module_genes, net)
heatmap(blk)
}

}
