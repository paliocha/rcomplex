% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcomplex-class.R
\name{rcomplex}
\alias{rcomplex}
\title{Create an rcomplex analysis object}
\usage{
rcomplex(
  species,
  traits,
  networks,
  orthologs,
  species_pairs = NULL,
  phylo_pairs = NULL
)
}
\arguments{
\item{species}{Character vector of species abbreviations.}

\item{traits}{Named character vector mapping species to trait labels
(e.g., \code{c(BDIS = "annual", BSYL = "perennial")}).}

\item{networks}{Named list of \code{\link{compute_network}} outputs,
keyed by species.}

\item{orthologs}{Data frame with columns \code{Species1},
\code{Species2}, \code{hog}.}

\item{species_pairs}{Optional list of length-2 character vectors
specifying species pairs for gene-level comparison. Defaults to
all pairwise combinations.}

\item{phylo_pairs}{Optional data frame with columns \code{sp1},
\code{sp2} (and optionally \code{pair_name}) for module-level
comparison.}
}
\value{
An S3 object of class \code{"rcomplex"}.
}
\description{
Constructs a container that carries species metadata, networks,
orthologs, and analysis results through the rcomplex pipeline.
Pipeline functions gain S3 methods that read inputs from the object
and store results back, eliminating repetitive argument passing.
}
\examples{
\dontrun{
rcx <- rcomplex(
  species = c("SP_A", "SP_B", "SP_C"),
  traits = c(SP_A = "annual", SP_B = "perennial", SP_C = "annual"),
  networks = list(SP_A = net_a, SP_B = net_b, SP_C = net_c),
  orthologs = ortho
)
rcx <- find_coexpressologs(rcx, method = "permutation")
rcx <- find_cliques(rcx)
rcx$edges     # access results directly
rcx$cliques
}

}
