% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preservation_matrix.R
\name{all_species_pairs}
\alias{all_species_pairs}
\title{All unordered species pairs as a \code{pairs} data frame}
\usage{
all_species_pairs(species, sep = ".")
}
\arguments{
\item{species}{Character vector of species identifiers. A \emph{named} vector
--- a \code{group} map, say --- is read as its names, since a bare species
vector is not normally named.}

\item{sep}{Separator used to build \code{pair_name} (default \code{"."}), matching
the \code{paste(sp1, sp2, sep = ".")} default of \code{\link[=preservation_paired]{preservation_paired()}}, so
the two agree when \code{pair_name} is left implicit.}
}
\value{
A data frame with columns \code{sp1}, \code{sp2} and \code{pair_name}, one row per
unordered pair. Both directions of each contrast are run by
\code{\link[=preservation_paired]{preservation_paired()}}, so each pair appears once.
}
\description{
Builds the \code{pairs} argument of \code{\link[=preservation_paired]{preservation_paired()}} for every
\code{choose(n, 2)} contrast, so an all-pairs preservation matrix does not have
to be hand-written. Running all pairs rather than a designated few is what
makes \code{\link[=preservation_matrix_test]{preservation_matrix_test()}} worth doing: every extra contrast adds
its modules to the class means the statistic is built from, and any
species the designated few left out widens the relabelling null as well.
Eight species split 4/4 give \code{choose(8, 4) = 70} free labellings, against
the 16 that a within-genus null over four genera of two can reach.
}
\examples{
all_species_pairs(c("BDIS", "BSYL", "HVUL"))

}
