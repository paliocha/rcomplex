% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-sparse.R
\name{as_sparse_network}
\alias{as_sparse_network}
\title{Convert a dense network object to the sparse representation}
\usage{
as_sparse_network(net, store_density = 0.05)
}
\arguments{
\item{net}{Dense network object: output of
\code{compute_network(sparse = FALSE)}, or a hand-built
\verb{list(network = <matrix>, threshold = )} with gene dimnames.}

\item{store_density}{Fraction of top edges to keep in the store
(default 0.05). Must be in (0, 1) and, when the network records its
density in \code{params$density}, at least that density (the store must
contain every analysis edge).}
}
\value{
The network object with \code{network} as a \code{dgCMatrix} plus
\code{store_density} and \code{store_threshold} fields.
}
\description{
Thresholds the dense co-expression matrix at the \code{store_density}
quantile and repacks the surviving off-diagonal entries (both
triangles) as a \code{Matrix::dgCMatrix}, exactly as
\code{compute_network(sparse = TRUE)} does. All other fields of \code{net} are
carried over; \code{store_density}, \code{store_threshold} and
\code{params$store_density} are added. Values below \code{store_threshold} are
discarded, so downstream analyses at a threshold below it are refused.
}
\examples{
\dontrun{
net <- compute_network(x, density = 0.03, sparse = FALSE)
net_sparse <- as_sparse_network(net, store_density = 0.05)
}

}
