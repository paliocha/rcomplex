% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_preservation.R
\name{classify_preservation}
\alias{classify_preservation}
\title{Classify modules as conserved, moderately preserved, or diverged}
\usage{
classify_preservation(
  pres,
  alpha = 0.1,
  z_conserved = 10,
  z_scale = c("standardized", "raw"),
  species = NA_character_,
  pair_name = NA_character_
)
}
\arguments{
\item{pres}{Output of \code{\link[=module_preservation]{module_preservation()}}.}

\item{alpha}{Significance threshold for the combined q-value (default 0.1).}

\item{z_conserved}{Cut point at or above which a significant module is
called conserved rather than moderately preserved (default 10).}

\item{z_scale}{Which \code{Zsummary} the cut point is read against.
\code{"standardized"} (default) uses \code{Zsummary_std}, which has unit variance
under the permutation null; \code{"raw"} uses \code{Zsummary} itself.

This matters because the familiar 10 / 2 cut points are not
scale-free. Langfelder et al. (2011) set them for a \code{Zsummary} built as
the mean of two medians over several density and connectivity statistics.
Here only two statistics are available from an adjacency matrix, so
\code{Zsummary} is the mean of two standardized values and its null spread is
\code{sqrt(2 + 2 * rho) / 2}, where \code{rho} is the null correlation between
\code{avg.weight} and \code{cor.degree} -- between 0.71 and 1 rather than the
smaller spread a median-of-several carries. Reading 10 against the raw
mean therefore imports a number calibrated on a different quantity.
Dividing by the null sd restores its intended meaning, "this many null
standard deviations". Use \code{"raw"} only to reproduce older output.}

\item{species}{Optional species label recorded in the \code{species} column.}

\item{pair_name}{Optional contrast label recorded in the \code{pair_name} column.}
}
\value{
A data frame with \code{module}, \code{species}, \code{pair_name},
\code{classification}, \code{Zsummary}, \code{Zsummary_std}, \code{medianRank}, \code{q.value},
\code{size} and \code{size_mapped}. \code{Zsummary_std} is the column the default
\code{z_scale = "standardized"} criterion is read against, so reproducing
the call from \code{Zsummary} alone will not match. \code{medianRank} is carried
through from \code{\link[=module_preservation]{module_preservation()}} and takes no part in the call; see
\emph{Ordering modules} for when to read it instead.
}
\description{
Turns \code{\link[=module_preservation]{module_preservation()}} output into a per-module call. The call is
driven by the combined permutation q-value; the \code{Zsummary} thresholds of
Langfelder et al. (2011) are applied as a secondary split among the
significant modules, so the familiar 2 / 10 cut points still appear.
}
\section{Criteria}{

\describe{
\item{conserved}{\code{q.value < alpha} and the \code{Zsummary} scale chosen by
\code{z_scale} is at or above \code{z_conserved}}
\item{moderate}{\code{q.value < alpha} and it is below \code{z_conserved}}
\item{diverged}{\code{q.value >= alpha}}
\item{untested}{\code{q.value} is \code{NA} -- a statistic could not be computed,
so neither preservation nor divergence was measured}
}
A module whose degree correlation could not be computed -- \code{cor.degree} is
undefined when intramodular connectivity is constant in either network --
gets \code{NA} for that statistic, so the \code{pmax} combination and hence \code{q.value}
are \code{NA} too. Such a module is reported \code{"untested"} rather than
\code{"diverged"}: nothing was measured, so divergence would be a positive claim
the data does not support. It is never called preserved on the density
statistic alone.
}

\section{Ordering modules}{

\code{Zsummary_std} decides the call, but it is not the only ordering worth
reading. It is a Z-score, so its denominator is a permutation standard
deviation, and that shrinks as a module grows: over 110 modules from 12
Pooideae contrasts the null sd of \code{cor.degree} tracks mapped module size
at Spearman -0.996 and that of \code{avg.weight} at -0.873. A large, weakly
preserved module can therefore outrank a small, strongly preserved one.

\code{medianRank} is the size-independent complement Langfelder et al. (2011)
report next to \code{Zsummary} for exactly that reason. It ranks the observed
\code{avg.weight} and \code{cor.degree} across the tested modules, 1 = strongest,
and averages the two ranks; no permutation moment enters it, so module
size cannot set its scale. Prefer \code{medianRank} when asking which modules
are the best preserved \emph{relative to each other}, and \code{Zsummary_std} when
asking how far from its own null any one module sits. The two are not
redundant: on those same 12 contrasts they ordered 11\% of module pairs
differently.

\code{medianRank} ranks across the modules of \emph{one run}. Its scale is set by
how many modules that run tested, so it is not comparable between runs --
between the two directions of a contrast, or between contrasts in a
\code{\link[=preservation_paired]{preservation_paired()}} table -- unless those runs tested equally many
modules. It takes no part in the classification.
}

\examples{
\dontrun{
cls <- classify_preservation(pres)
table(cls$classification)
}

}
