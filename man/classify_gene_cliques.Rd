% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clique_gene_graph.R
\name{classify_gene_cliques}
\alias{classify_gene_cliques}
\title{Classify gene-graph cliques into conservation tiers}
\usage{
classify_gene_cliques(
  cliques,
  edges,
  species,
  lineage = NULL,
  alpha_call = 0.1,
  alpha_graph = 0.9,
  max_gap = 1L,
  cross_max = NULL
)
}
\arguments{
\item{cliques}{Data frame from \code{\link{gene_clique_graph}}, or
any table with \code{clique_id}, \code{hog}, \code{species} and \code{gene} columns.
Combine runs at several \code{alpha_graph} values (with distinct
\code{id_prefix}) to expose every tier: a clique complete at
\code{alpha_call} need not be maximal on a looser graph. Two runs that
collide on a \code{clique_id} are refused rather than merged.}

\item{edges}{The full, unfiltered co-expressolog table. It must not
be pre-filtered on \code{q.value}: the gap tier needs to see rows that
were tested and failed in order to refuse them.}

\item{species}{Character vector of every species in the analysis.
Every species appearing in \code{cliques} must be listed; a stranger
would be counted into the clique's species total while also being
reported as missing.}

\item{lineage}{Optional named vector mapping each species to a
lineage. Required for \code{lineage_specific} and \code{differentiated};
when \code{NULL} those two tiers are skipped.}

\item{alpha_call}{Significance threshold for calling a species pair
conserved (default 0.1).}

\item{alpha_graph}{Loose threshold defining the \code{partial_significant}
graph (default 0.9).}

\item{max_gap}{Largest number of absent species tolerated by
\code{partial_present} (default 1).}

\item{cross_max}{Maximum number of significant cross-lineage pairs
allowed by \code{differentiated}. Defaults to \code{choose(S - 1, 2) - W},
the generalisation of the published cut; the original six-species
script used a looser hard-coded 6.}
}
\value{
A data frame with one row per clique:
\describe{
\item{clique_id, hog}{Clique identity}
\item{classification}{Tier, or \code{"unclassified"}}
\item{hog_class}{Earliest tier reached by any clique of that HOG,
reproducing the HOG-level precedence of the published scripts
without discarding the per-clique detail}
\item{n_members, n_species}{Clique size}
\item{n_pairs, n_present, n_sig}{Member pairs, pairs with a row
in \code{edges}, and pairs significant at \code{alpha_call}}
\item{n_sig_within, n_sig_cross}{Significant pairs within and
across lineages (\code{NA} without \code{lineage})}
\item{n_missing, missing_species, missing_reason}{Species not in
the clique, and why: \code{absent} (no row in the HOG),
\code{untested} (in the HOG but never compared to a member),
\code{tested_ns} (compared and not significant), \code{extendable}
(significant against every member, so the clique came from a
looser graph). Comma-separated and positionally aligned}
\item{mean_q, max_q}{Recomputed from \code{edges} over present pairs}
\item{mean_effect_size}{Present when \code{edges} carries
\code{effect_size}; the ranking key to prefer, since \code{mean_q}
saturates}
\item{mean_q_floor, n_cliques_at_q_floor}{Smallest \code{mean_q} over
the classified cliques and how many are tied there -- the
resolution \code{mean_q} actually has as a ranking key}
}
The two floor diagnostics are also attributes, alongside
\code{n_species_total}, \code{n_pairs_total}, \code{n_within_pairs},
\code{n_cross_pairs}, \code{cross_max}, \code{alpha_call}, \code{alpha_graph} and
\code{max_gap}. All are set even when the result is empty.
}
\description{
Applies the published five-tier taxonomy to the cliques returned by
\code{\link{gene_clique_graph}}. Every threshold is derived from
the number of species actually supplied, so nothing is tied to the
six species and fifteen species-pairs of the original workflow.
}
\details{
With \code{S = length(species)} species, \code{P = choose(S, 2)} species-pairs,
lineage sizes \code{n_l}, \code{W = sum(choose(n_l, 2))} within-lineage pairs
and \code{X = P - W} cross-lineage pairs, the tiers are:
\describe{
\item{complete_conserved}{All \code{S} species present and all
\code{choose(S, 2)} pairs significant at \code{alpha_call}.}
\item{lineage_specific}{A complete clique over one entire lineage,
with no species outside it testable against the clique. A species
that \emph{was} compared against every member and came back
non-significant is evidence of a boundary rather than a gap, so
it blocks this tier -- that clique is a candidate for
\code{differentiated}, on the cliques of the unfiltered graph.}
\item{partial_significant}{All \code{S} species present, every clique
edge below \code{alpha_graph}, and at least \code{choose(S - 1, 2) + 1}
pairs significant at \code{alpha_call}.}
\item{partial_present}{\code{S - g} species present for
\verb{1 <= g <= max_gap}, all \code{choose(S - g, 2)} pairs significant,
and every absent species an annotation gap rather than a
rejected test.}
\item{differentiated}{All \code{S} species present, every pair tested,
at least one lineage of two or more species, every such lineage
fully significant within itself, and at most \code{cross_max}
cross-lineage pairs significant.}
}
The waterfall is evaluated in that order and the first match wins.

\code{choose(S - 1, 2) + 1} equals \code{choose(S, 2) - (S - 2)}, so the
\code{partial_significant} tolerance is \code{S - 2} non-significant edges.
That is the largest tolerance under which no member can be isolated:
cutting a member loose needs all \code{S - 1} of its edges removed, so
with \code{S - 2} removals every member still keeps a significant edge.

\code{differentiated} demands that every member pair actually has a row in
\code{edges}. Without that, a clique whose cross-lineage pairs were never
tested would be scored as diverged on absent evidence -- the same
conflation \code{partial_present} refuses through \code{missing_reason}.
}
\examples{
edges <- data.frame(
  gene1 = c("a1", "a1", "b1"), gene2 = c("b1", "c1", "c1"),
  species1 = c("SP_A", "SP_A", "SP_B"),
  species2 = c("SP_B", "SP_C", "SP_C"),
  hog = "HOG1", q.value = c(0.01, 0.02, 0.03)
)
cl <- gene_clique_graph(edges)
classify_gene_cliques(cl, edges, c("SP_A", "SP_B", "SP_C"))

}
\references{
Rodriguez E, Birkeland S, Chapple ED, et al. (2026).
Comparative regulomics of wood formation across dicot and
conifer trees. \emph{Nature Communications} 17(1).
\doi{10.1038/s41467-026-75624-2}
}
\seealso{
\code{\link{gene_clique_graph}}
}
