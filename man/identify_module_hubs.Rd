% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules.R
\name{identify_module_hubs}
\alias{identify_module_hubs}
\title{Identify hub genes within co-expression modules}
\usage{
identify_module_hubs(
  modules,
  net,
  orthologs = NULL,
  comparison = NULL,
  centrality = c("degree", "betweenness", "eigenvector"),
  top_n = NULL,
  top_fraction = 0.1,
  min_module_size = 3L
)
}
\arguments{
\item{modules}{Output of \code{\link[=detect_modules]{detect_modules()}}.}

\item{net}{Output of \code{\link[=compute_network]{compute_network()}}.}

\item{orthologs}{Optional data frame from \code{\link[=parse_orthologs]{parse_orthologs()}} with columns
\code{Species1}, \code{Species2}, \code{hog}.  The function auto-detects which column
matches the gene names in \code{modules}.  If \code{NULL}, the \code{hog} column in the
result is all \code{NA}.}

\item{comparison}{Optional data frame: the \verb{$results} element from
\code{\link[=summarize_comparison]{summarize_comparison()}}.  When provided, enables conservation-informed
tie-breaking (tiers 5--6).  Must contain columns \code{Species1}, \code{Species2},
\code{hog}, \code{Species1.effect.size}, \code{Species2.effect.size}, plus at least one
pair of q-value columns (\code{Species1.q.val.con}/\code{Species2.q.val.con} or
the \code{.div} variants).}

\item{centrality}{Centrality measure: \code{"degree"} (default), \code{"betweenness"},
or \code{"eigenvector"}.}

\item{top_n}{Integer: flag the top N genes per module as hubs.  If \code{NULL}
(default), uses \code{top_fraction} instead.}

\item{top_fraction}{Numeric in (0, 1): fraction of genes per module to flag
as hubs (default 0.1).  Ignored when \code{top_n} is non-NULL.}

\item{min_module_size}{Integer: modules with fewer genes get
\code{is_hub = FALSE} for all genes (default 3).}
}
\value{
A data frame with one row per gene, ordered by module then rank:
\describe{
\item{gene}{Gene identifier}
\item{module}{Module ID (integer)}
\item{degree}{Within-module weighted degree (\code{igraph::strength})}
\item{betweenness}{Within-module betweenness centrality}
\item{eigenvector}{Within-module eigenvector centrality}
\item{mean_edge_weight}{Mean weight of edges to other module members}
\item{global_degree}{Weighted degree in the full (thresholded) network}
\item{rank}{Rank within module by primary centrality
(1 = highest; ties use \code{"min"})}
\item{is_hub}{\code{TRUE} if the gene is in the top slice after the
6-tier tie-breaking cascade}
\item{hog}{HOG identifier (\code{NA} if \code{orthologs} not provided or gene
not in the ortholog table)}
}
}
\description{
Computes within-module centrality for each gene and flags the top-ranked
genes as hubs.  Optionally maps genes to ortholog groups (HOGs) for
downstream conservation analysis with \code{\link[=classify_hub_conservation]{classify_hub_conservation()}}.
}
\section{Centrality measures}{

Centrality is computed on the \strong{within-module subgraph} (edges between
genes in the same module only):
\describe{
\item{degree}{Weighted degree (\code{igraph::strength}): sum of edge weights
to other genes in the same module.}
\item{betweenness}{Shortest-path betweenness using inverse edge weights
as distances.  Identifies genes that bridge sub-clusters within a module.}
\item{eigenvector}{Eigenvector centrality (\code{igraph::eigen_centrality}):
high for genes connected to other high-centrality genes.}
}
}

\section{Tie-breaking cascade}{

When genes share the same primary centrality score, hub selection uses a
biologically informed cascade (all available tiers evaluated):
\enumerate{
\item Primary centrality (user-selected measure)
\item Global weighted degree across the full network
\item Alternative within-module centrality (betweenness if primary is
degree; degree otherwise)
\item Mean within-module edge weight (strength / degree)
\item Per-gene conservation effect size (requires \code{comparison})
\item Per-HOG minimum q-value (requires \code{comparison}; lower = better)
}
}

\examples{
\dontrun{
hubs <- identify_module_hubs(modules, net, orthologs,
                             comparison = summary$results)
hubs[hubs$is_hub, ]
}

}
